#' @param k Integer specifying the number of folds for cross-validation.
#' An alternative to setting parameter `cross_validation_df` for performing cross-validation; if both are missing no cross-validation is used.
