#' @param cv_name Character string specifying the column name in \code{data}
#' which contains the cross-validation fold. Leaving this parameter empty (default) means that
#' no cross-validation will be performed.
